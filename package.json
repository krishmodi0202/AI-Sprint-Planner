{
  "name": "ai-sprint-planner",
  "version": "1.0.0",
  "description": "AI-powered sprint planning tool that converts PRDs to structured tasks",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["ai", "sprint", "planning", "prd", "trello"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
